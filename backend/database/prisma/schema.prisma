generator client {
  provider     = "prisma-client"
  output       = "generated"
  moduleFormat = "esm"
  engineType   = "binary"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
